version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctf_web
    ports:
      - "80:80"     
    volumes:
      - ./src:/var/www/html
    restart: unless-stopped
